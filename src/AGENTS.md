# AGENTS — `src`

- **`sunspot/`** — the only importable project package (`import sunspot`, `from sunspot...`).
- Parent project metadata is in the repository [pyproject.toml](../pyproject.toml); console entry `sunspot` points at `sunspot.cli:main`.
